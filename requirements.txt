# Core dependencies for BTCBeeper TUI application
websockets==12.0
textual==4.0.0
pygame==2.6.1

# Optional dependencies for click_generator.py (sound file generation)
numpy==2.3.1
scipy==1.16.0

# Testing dependencies
pytest==9.0.2
pytest-asyncio==1.3.0